extends layout

block content
  link(rel='stylesheet', href='/css/index.css')
  This is the homepage of <a href='http://github.com/sharadmv'>Sharad Vikram</a>. It is not yet complete, so please wait a bit.
  //-#blog(ng-app='blog', ng-controller='BlogControl')
    //-div(ng-repeat='post in posts')
      //-a(href='/blog/{{post._id}}')
        //-{{post.title}} 
      //-markdown-short(ng-model='post.description')
