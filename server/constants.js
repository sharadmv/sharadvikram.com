var init = function(app) {
  var interface = {
    namespace : {
      TEST : "test",
      BETA : "beta"
    },
    tag : {
      SERVER : "SERVER"
    }
  }
  return interface;
}
module.exports = init;
